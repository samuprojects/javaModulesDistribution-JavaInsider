package theory;
@SuppressWarnings("ALL")
public class CreatingModules {

    /*
    * Criar módulos dentro de um projeto está muito relacionado com a IDE escolhida.
    * Para genrenciar os módulos de uma aplkicação no Intellij é necessário:
    * Com o botão do mouse sobre o projeto New / Module...
    * Um module nada mais é do que um componente de software que possui um nome (dentro dele serão criados pacotes, classes, etc).
    * Esse nome pode ser uma palavra qualquer, porém, geralmente se usa do domínio da aplicação, exemplo, org.app.security semelhante a estrutura dos pacotes
    * Nesse primeiro momento o professor sugeriu nomear com apenas uma palavra "app".
    * Depois clicar em Create.
    * É muito importante colocar as classes que estão dentro do módulo recém criado em pacotes para evitar problemas no futuro em alguns cenários.
    * Professor optou pelo exemplo: br.com.myapp.app
    * Agora como exemplo num sistema de segurança vamos simular uma autenticação simples apenas para mostrar o conceito do funcionamento do módulo criado.
    * Com isso ao invés de criar o código diretamente no projeto, foi desenvolvido num módulo chamado app, e dentro dele, pacotes, classes.
    *
    * */
}
